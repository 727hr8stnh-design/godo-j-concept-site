# 合同自動車コンセプトサイト開発ルール

このリポジトリでは共通開発憲法に加え、以下を守る。

## 目的と利用者

- 合同自動車のサービス、会社情報、問い合わせ導線を正確で分かりやすく伝える。
- 車やWebに詳しくない利用者でも、スマートフォンとPCから迷わず利用できるようにする。
- 現在公開中の内容や既存導線を、依頼なしに削除・変更しない。

## 実装方針

- 既存のHTML、CSS、JavaScriptによる静的構成を尊重する。
- 共通要素の変更前に、影響を受ける全ページを検索する。
- 文言、電話番号、営業時間、価格、サービス内容など業務情報を推測で補わない。
- 画像は利用権、出典、加工可否を確認し、既存の画像取扱いルールを守る。
- モバイル表示を主対象の一つとして扱い、PC表示とともに確認する。

## 承認が必要な操作

- 公開サイトへのデプロイまたは公開内容の切替
- 問い合わせフォームからの実送信
- 電話番号、営業時間、住所、価格など重要な業務情報の変更
- 外部サービス、アクセス解析、広告、Cookie、個人情報収集の追加
- 公開ファイルの削除、ドメイン・DNS・リポジトリ設定の変更

## 検証

- 変更ページをブラウザで実際に表示する。
- 主要ページ間のリンク、ナビゲーション、問い合わせ導線を確認する。
- モバイル幅とPC幅で、はみ出し、重なり、読みにくさがないことを確認する。
- JavaScript変更時はブラウザコンソールエラーを確認する。
- 公開後はHTTP応答だけでなく、変更した文言または見た目が公開URLへ反映されたことを確認する。
- ローカル確認、Git push、公開反映、公開内容確認を別々に記録する。

## 完了条件

- 依頼された変更が実装されている。
- 既存の主要導線が維持されている。
- PCとモバイルで対象ページを確認している。
- 重要情報を根拠と照合している。
- 必要な仕様・進捗文書を更新している。
- 公開を依頼された場合は、公開URLで変更内容まで確認している。
